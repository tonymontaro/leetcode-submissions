{"id":453258653,"lang":"python3","time":"4 months, 3 weeks","timestamp":1612717043,"status_display":"Accepted","runtime":"72 ms","url":"/submissions/detail/453258653/","is_pending":"Not Pending","title":"Dungeon Game","memory":"15.2 MB","code":"from math import inf\n\nclass Solution:\n    def calculateMinimumHP(self, grid: List[List[int]]) -> int:\n        rlen, clen = len(grid), len(grid[0])\n        for r in range(rlen-1, -1, -1):\n            for c in range(clen-1, -1, -1):\n                if (r, c) == (rlen-1, clen-1):\n                    grid[r][c] = max(1, -grid[r][c] + 1)\n                    continue\n                \n                val = -grid[r][c]\n                needed = inf\n                if r < rlen-1:\n                    needed = max(1, val + grid[r+1][c])\n                if c < clen-1:\n                    needed = min(needed, max(1, val + grid[r][c+1]))\n                grid[r][c] = needed\n\n        return grid[0][0]","compare_result":"111111111111111111111111111111111111111111111","title_slug":"dungeon-game"}