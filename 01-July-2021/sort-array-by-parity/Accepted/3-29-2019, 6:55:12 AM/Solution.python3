// https://leetcode.com/problems/sort-array-by-parity

class Solution:
    def sortArrayByParity(self, arr: List[int]) -> List[int]:
        even = [el for el in arr if el % 2 == 0]
        odd = [el for el in arr if el % 2 == 1]
        return even + odd