{"id":459470543,"lang":"python3","time":"4 months, 1 week","timestamp":1614044622,"status_display":"Accepted","runtime":"28 ms","url":"/submissions/detail/459470543/","is_pending":"Not Pending","title":"Construct the Lexicographically Largest Valid Sequence","memory":"14.3 MB","code":"class Solution:\n    def constructDistancedSequence(self, n: int) -> List[int]:\n        nums = n\n        n = n * 2 - 1\n        self.ans = []\n        def solve(idx, seen, arr):\n            # print(idx, seen, arr)\n            if idx >= n:\n                self.ans = list(arr)\n                return True\n            if arr[idx] != -1:\n                return solve(idx + 1, seen, arr)\n            \n            for i in range(nums, 0, -1):\n                if i in seen:\n                    continue\n                if i > 1 and idx + i >= n:\n                    return False\n                if i > 1 and arr[idx + i] != -1:\n                    continue\n                seen.add(i)\n                arr[idx] = i\n                if i > 1:\n                    arr[idx + i] = i\n                if solve(idx + 1, seen, arr):\n                    return True\n                seen.remove(i)\n                arr[idx] = -1\n                if i > 1:\n                    arr[idx + i] = -1\n            return False\n        \n        solve(0, set(), [-1] * n)\n        return self.ans","compare_result":"11111111111111111111","title_slug":"construct-the-lexicographically-largest-valid-sequence"}