{"id":411842427,"lang":"python3","time":"8 months, 1 week","timestamp":1603364307,"status_display":"Accepted","runtime":"60 ms","url":"/submissions/detail/411842427/","is_pending":"Not Pending","title":"Evaluate Reverse Polish Notation","memory":"14.6 MB","code":"class Solution:\n    def evalRPN(self, tokens: List[str]) -> int:\n        signs = {\n            \"+\": lambda x, y: x + y,\n            \"-\": lambda x, y: x - y,\n            \"*\": lambda x, y: x * y,\n            \"/\": lambda x, y: 0 if y == 0 or x == 0 else ((x * y)//(abs(x * y))) * (abs(x)//abs(y))\n        }\n        \n        res = []\n        for item in tokens:\n            if item in signs:\n                b = res.pop()\n                a = res.pop()\n                res.append(signs[item](a, b))\n            else:\n                res.append(int(item))\n        return res[0]","compare_result":"11111111111111111111","title_slug":"evaluate-reverse-polish-notation"}