{"id":411324303,"lang":"python3","time":"8 months, 1 week","timestamp":1603255308,"status_display":"Accepted","runtime":"52 ms","url":"/submissions/detail/411324303/","is_pending":"Not Pending","title":"Add Bold Tag in String","memory":"15 MB","code":"class Solution:\n    def addBoldTag(self, s: str, dct: List[str]) -> str:\n        # O(len(s) * len(dct)*wordLn)\n        n = len(s)\n        ranges = []\n        for word in dct:\n            idx = 0\n            while idx != -1 and idx < n:\n                idx = s.find(word, idx)\n                if idx != -1:\n                    ranges.append((idx, idx + len(word)))\n                    idx += 1\n        if not ranges:\n            return s\n        # print(ranges)\n        ranges.sort()\n        final = []\n        for start, end in ranges:\n            if final and start <= final[-1][1]:\n                prevS, prevE = final.pop()\n                final.append((prevS, max(prevE, end)))\n            else:\n                final.append((start, end))\n        res = \"\"\n        prev = 0\n        # print(final)\n        for start, end in final:\n            res += s[prev: start] + f\"<b>{s[start: end]}</b>\"\n            prev = end\n        res += s[prev:len(s)]\n        return res","compare_result":"1111111111111111111111111111111111111111111111111111111111111111111111","title_slug":"add-bold-tag-in-string"}