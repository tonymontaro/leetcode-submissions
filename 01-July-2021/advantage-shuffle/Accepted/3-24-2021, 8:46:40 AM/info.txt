{"id":471786139,"lang":"cpp","time":"3 months, 1 week","timestamp":1616572000,"status_display":"Accepted","runtime":"56 ms","url":"/submissions/detail/471786139/","is_pending":"Not Pending","title":"Advantage Shuffle","memory":"35.6 MB","code":"#define v vector\n\nclass Solution {\npublic:\n    vector<int> advantageCount(vector<int>& A, vector<int>& B) {\n        v<pair<int, int>> arr;\n        for (int i = 0; i < A.size(); i++) arr.push_back({B[i], i});\n        sort(arr.begin(), arr.end());\n        sort(A.begin(), A.end());\n        int idx = 0, ridx = 0;\n        v<int> rem;\n        v<int> ans(B.size(), -1);\n        for (auto x: arr) {\n            while (idx < A.size() && A[idx] <= x.first) {\n                rem.push_back(A[idx]);\n                idx++;\n            }\n            if (idx < A.size())\n                ans[x.second] = A[idx++];\n            else\n                ans[x.second] = rem[ridx++];\n        }\n        return ans;\n    }\n};\n","compare_result":"1111111111111111111111111111111111111111111111111111111111111111111","title_slug":"advantage-shuffle"}